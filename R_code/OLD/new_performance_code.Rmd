---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
library(tidyverse)
library(survival)
library(survminer)
library(cmprsk)
library(tidyverse)
library(caret)
library(survival)
library(survminer)
library(lubridate)
```

```{r}
features = read_tsv("/Users/noa/Workspace/raxml_deep_learning_results/ready_raw_data/Pandit/ML/all_features.tsv")
```
```{r}
features %>% distinct (feature_msa_pypythia_msa_difficulty, feature_mds_False_stress_20) %>% ggplot(aes(x=feature_msa_pypythia_msa_difficulty, y=feature_mds_False_stress_20))+geom_point()

features %>% distinct (msa_path, feature_msa_pypythia_msa_difficulty)# %>% arrange(-feature_msa_pypythia_msa_difficulty)
```


```{r}
features %>% group_by(msa_path,starting_tree_ind,starting_tree_type,feature_mds_True_pca_0_3,feature_mds_True_pca_1_3,feature_mds_True_pca_2_3) %>% summarise(is_global_max = max(delta_ll_from_overall_msa_best_topology<=0.1)) %>% filter (starting_tree_type=='rand') %>% filter(msa_path=='/groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF07733.fasta')  %>% ggplot(aes(x=feature_mds_True_pca_1_3, y = feature_mds_True_pca_2_3, color =as.factor(is_global_max), shape = starting_tree_type))+geom_point()

#features %>% filter(msa_path=='/groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF00083.fasta') 
```
```{r}
features %>% group_by(msa_path,starting_tree_ind,starting_tree_type,feature_mds_True_pca_0_3,feature_mds_True_pca_1_3,feature_mds_True_pca_2_3) %>% summarise(is_global_max = max(delta_ll_from_overall_msa_best_topology<=0.1)) %>% filter(msa_path=='/groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF01416.fasta') 
```


```{r}
summary (data %>% pull (feature_msa_pypythia_msa_difficulty))
```


```{r}
performance_data

performance_data %>% ggplot(aes(x=threshold, y = mean_status, color = metric)) +expand_limits(y = 0)+geom_point()+ geom_line()+geom_abline(slope=1, intercept = 0, color = 'black') 


performance_data %>% ggplot(aes(y=mean_status-mean_default_status, x = mean_time,color = metric)) +expand_limits(y = 0)+ geom_line()

performance_data %>% ggplot(aes(y=mean_LL_diff-mean_default_diff, x = mean_time,color = metric)) +expand_limits(y = 0)+ geom_line()
```
```{r}
performance_data %>%  ggplot(aes(x = threshold, y = mean_status )) + geom_point()
```



Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Cmd+Option+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Cmd+Shift+K* to preview the HTML file). 

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.

