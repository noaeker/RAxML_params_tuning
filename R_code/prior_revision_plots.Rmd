---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
library(scales)
library(tidyverse)
library(caret)
library(ggpubr)
library(pROC)
```


```{r}
prefix = "/Users/noa/Workspace/raxml_deep_learning_results/groups_new_mixed_v_1/"
full_data = read_tsv(paste(prefix,"group_results.tsv", sep = ""))

test_data = read_tsv(paste(prefix,"final_performance_on_test_M_frac_1.0_RFE_True_large_grid_True_out_features_True.tsv",sep = ""))
val_data = read_tsv(paste(prefix,"final_performance_on_val_M_frac_1.0_RFE_True_large_grid_True_out_features_True.tsv",sep = ""))
error_vs_size = read_tsv(paste(prefix,"group_classification_metrics.tsv",sep = ""))
error_vs_group = read_tsv(paste(prefix,"group_classification_group_metrics_M_frac_1.0_RFE_True_large_grid_True_out_features_True.tsv",sep = ""))
```

```{r}
summary(val_data %>% pull (default_status))
```

Data statistics

Data Per Source
```{r}
full_data %>%  filter (grepl('Pandit',msa_path)) %>% distinct (msa_path) 
full_data %>%  filter (grepl('Selectome',msa_path)) %>% distinct (msa_path) 
full_data %>%  filter (grepl('DNA',msa_path)) %>% distinct (msa_path)
full_data %>%  filter (grepl('PROTEIN',msa_path)) %>% distinct (msa_path)
```


```{r}

summary(full_data %>% pull (feature_msa_n_loci))

summary(full_data %>% pull (default_status))

data<-full_data %>% group_by(msa_path) %>% summarise(mean_prob = mean(default_status))

summary(data %>% pull (mean_prob))

data
(data %>% filter (mean_prob>0.9))
(data %>% filter (mean_prob>0.5))
(data %>% filter (mean_prob<0.1))
(data %>% filter (mean_prob<0.05))


```


Confusion matrix
```{r}

ggplotConfusionMatrix <- function(m){
  mytitle <- ""#paste("Accuracy", percent_format()(m$overall[1]))
                  # "Kappa", percent_format()(m$overall[2]))
  p <-
    ggplot(data = as.data.frame(m$table) ,
           aes(x = Reference, y = Prediction)) +
    geom_tile(aes(fill = log(Freq)), colour = "white") +
    scale_fill_gradient(low = "white", high = "steelblue") +
    geom_text(aes(x = Reference, y = Prediction, label = Freq)) +
    theme(legend.position = "none") +
    ggtitle(mytitle)+theme(text = element_text(size = 13))
  return(p)
}
conf_data<-test_data %>% dplyr::select (uncalibrated_prob, default_status) %>% mutate (pred_status = round(uncalibrated_prob)) 

obs<- factor(conf_data %>% pull (default_status))
pred<- factor(conf_data %>% pull (pred_status))


confusion_plot<-ggplotConfusionMatrix(confusionMatrix(data = pred, reference= obs))+theme(legend.position = "none")
confusion_plot
```
Error vs size
```{r}
library(predtools)
library(MASS)
library(caret)


error_vs_size_plt = error_vs_size %>% mutate(n_MSAs = round(868*sample_fraction)) %>% ggplot(aes(x=n_MSAs, y = AUC))+geom_line()+ expand_limits(y = 0.5)+xlab("Number of MSAs")+ theme(text = element_text(size = 13))+ylab("AUC")+geom_point()

rocobj <- roc(test_data$default_status, test_data$calibrated_prob)
auc <- round(auc(test_data$default_status, test_data$calibrated_prob),3)

global_max_auc_plt<-ggroc(rocobj,colour = 'steelblue', size = 2)+
  ggtitle(paste0('AUC = ', auc))+theme(text = element_text(size = 13))+xlab("Specificity")


calibration_plt<-xyplot(calPlotData, auto.key = list(columns = 2),xlab="Observed",ylab="Predicted")



ggarrange(global_max_auc_plt,confusion_plot,error_vs_size_plt, calibration_plt ,labels = c("A","B","C","D"),align = "h", nrow = 2, ncol = 2 ,vjust= 1)


test_data%>% group_by (default_status) %>% count()

```




```{r}
error_vs_group %>% filter (grouping_col_name =='n_seq_group')  %>% distinct (grouping_col)
```

```{r}
library(lme4)


data_for_glm<-full_data %>% dplyr::select (msa_path, feature_msa_n_seq, spr_radius,spr_cutoff,n_rand_trees_sampled,n_pars_trees_sampled, default_status, feature_msa_pypythia_msa_difficulty) %>% mutate(default_status = as.factor(default_status)) %>% mutate(across(where(is.numeric), scale))

model<-glmer(formula = default_status ~ 1 + feature_msa_n_seq+(spr_radius+spr_cutoff+n_rand_trees_sampled+n_pars_trees_sampled)*feature_msa_n_seq+ (1|msa_path),
                           data    = data_for_glm,family = binomial) #to run the model
print(summary(model))
```
SVM 

Success
```{r}
#/groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF00001/.fasta // /groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF00002.fasta- Good
#/groups/pupko/noaeker/data/New_MSAs/Pandit_msas/PF00006.fasta - Bad

good_svm<- read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/curr_svm_plot_success2.csv")
X<-read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/X_svm_success2.csv")
Y<-read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/Y_svm_success2.csv")
X$Success = as.factor(Y$is_best_tree)



svm_success<-ggplot(data=good_svm,aes(x=xx,y=yy,z=Z))+ geom_contour_filled(show.legend=FALSE)+geom_point(data=X,size=2.5,mapping=aes(x=x,y=y,z=0, color=Success))+scale_color_manual(values = c("0" = "white", "1" = "red"))+labs(color='Is best final tree')+ xlab("PC1")+ylab("PC2")+theme(text = element_text(size = 11))
svm_success



```
Failure 
```{r}
bad_svm<- read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/curr_svm_plot_failure.csv")
bad_X<-read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/X_svm_failure.csv")
bad_Y<-read_csv("/Users/noa/Workspace/RAxML_params_tuning/groups_paper_ML_code/Y_svm_failure.csv")
bad_X$Success = as.factor(bad_Y$is_best_tree)


svm_failure<-ggplot(data=bad_svm,aes(x=xx,y=yy,z=Z))+ geom_contour_filled(show.legend=FALSE)+geom_point(data=bad_X,size=2.5,mapping=aes(x=x,y=y,z=0,color=Success))+scale_color_manual(values = c("0" = "white", "1" = "red"))+ xlab("PC1")+ylab("PC2")+labs(color='Is best final tree')+theme(text = element_text(size = 11))
svm_failure



```

```{r}
svm_example<-ggarrange(svm_success, svm_failure, labels = c("A1","A2"),nrow=2,common.legend = TRUE, legend="bottom", vjust=1,hjust=0.05)
svm_example
```


```{r}
error_vs_group %>% filter (grouping_col_name=='feature_msa_n_loci')






n_loci_error
```


```{r}
library(dplyr)
library(MASS)

Pypythia_error<-error_vs_group %>% filter (grouping_col_name =='msa_difficulty_group') %>% ggplot(aes(y=grouping_col, x=AUC))+geom_col()+ expand_limits(y = c(0.5,1))+ylab('MSA difficulty')+ xlab('AUC')+theme(text = element_text(size = 9))


n_seq_error<-error_vs_group %>% filter (grouping_col_name =='n_seq_group') %>% mutate(grouping_col = factor(grouping_col, levels = c('(29.999, 45.0]','(45.0, 67.0]','(67.0, 108.0]','(108.0, 199.0]'))) %>% ggplot(aes(y=grouping_col, x=AUC))+geom_col()+ expand_limits(y = c(0.5,1))+ylab('# Sequences')+xlab('AUC')+theme(text = element_text(size = 9))

n_loci_error<-error_vs_group %>% filter (grouping_col_name =='feature_msa_n_loci')  %>%  mutate(grouping_col = factor(grouping_col, levels = c('(99.999, 239.0]','(239.0, 632.0]','(632.0, 1437.0]','(1437.0, 12478.0]'))) %>% ggplot(aes(y=grouping_col, x=AUC))+geom_col()+ expand_limits(y = c(0.5,1))+ylab('# Positions')+xlab('AUC')+theme(text = element_text(size = 9))


Pypythia_scatter<-test_data %>% dplyr::select (msa_path,uncalibrated_prob,calibrated_prob,  feature_msa_pypythia_msa_difficulty) %>% group_by(msa_path,feature_msa_pypythia_msa_difficulty) %>% summarise(mean_prob = mean(calibrated_prob)) %>% ggplot(aes(x=feature_msa_pypythia_msa_difficulty, y = mean_prob))+geom_point(size = 0.7)+xlab("MSA difficulty")+ylab("Predicted probability")+theme(text = element_text(size = 9))

seq_scatter<-test_data %>% dplyr::select (msa_path,uncalibrated_prob,calibrated_prob, feature_msa_n_seq, feature_msa_pypythia_msa_difficulty) %>% group_by(msa_path,feature_msa_n_seq) %>% summarise(mean_prob = mean(calibrated_prob)) %>% ggplot(aes(x=feature_msa_n_seq, y = mean_prob))+geom_point(size = 0.7)+xlab("Number of sequences")+ylab("Predicted probability")+theme(text = element_text(size = 9))

n_loci_scatter<-test_data %>% dplyr::select (msa_path,uncalibrated_prob,calibrated_prob, feature_msa_n_loci) %>% group_by(msa_path,feature_msa_n_loci) %>% summarise(mean_prob = mean(calibrated_prob)) %>% ggplot(aes(x=feature_msa_n_loci, y = mean_prob))+geom_point(size = 0.7)+xlab("Number of positions")+ylab("Predicted probability")+theme(text = element_text(size = 9))


ggarrange(seq_scatter,Pypythia_scatter,n_loci_scatter, n_seq_error,Pypythia_error, n_loci_error,labels = c("A","B","C","D","E","F"),align = "h", nrow = 3, ncol = 3, legend = "bottom",vjust= 1)
#error_vs_group_5_5_including_plt


cor.test(test_data$feature_msa_pypythia_msa_difficulty, test_data$uncalibrated_prob)
cor.test(test_data$feature_msa_n_seq, test_data$uncalibrated_prob)
```


Investigate three most important features

```{r}
test_data$default_status_name<- if_else(test_data$default_status==0,'Local maximum','Global maximum')

svm_plt<-test_data %>% ggplot(aes(x=feature_final_trees_level_distances_embedd_PCA_0.9_rbf_svc_max_best_score, y =default_status_name))+geom_boxplot() +xlab("Maximal SVM decision function\n score within best final trees")+ylab("")

svm_plt



ggarrange(svm_example, svm_plt, labels = c("","B"),ncol=2)

#ggarrange(mean_rf_plt,min_pca_plt,mim_rf_pars,svm_plt,labels = c("A","B","C","D"),align = "h", nrow = 4, ncol = 1, legend = "bottom",vjust= 1)


```

```{r}

```




```{r}
hist(full_data %>% filter (default_status==1) %>% pull (feature_final_trees_level_distances_embedd_PCA_0.9_SVR_reg)) 
hist(full_data %>% filter (default_status==0) %>% pull (feature_final_trees_level_distances_embedd_PCA_0.9_SVR_reg)) 
```



Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Cmd+Option+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Cmd+Shift+K* to preview the HTML file). 

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.

